{
  "name": "codex-control-plane",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --watch server.mjs",
    "desktop": "electron electron.mjs",
    "start": "node server.mjs",
    "test": "node --test"
  },
  "devDependencies": {
    "electron": "^39.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
